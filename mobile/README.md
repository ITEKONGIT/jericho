# Mobile Application Assessment

Tooling for Android/iOS application penetration testing.

* `/network/persist_proxy.sh` - Uses ADB and iptables to forcefully route all Android device HTTP/HTTPS traffic to an intercepting proxy, bypassing standard app-level proxy configurations. Requires a rooted Android device or emulator.
